---
title: Reports
description: Weekly email digests and monthly PDF deep dives, scheduled in each
  restaurant's local timezone.
---

import { ChartLine, Rocket, Target } from 'lucide-react';

MenuWright turns its analysis into two recurring reports, scheduled around each restaurant's own timezone.

<Callout>
  Reports are sent to every plan. Free accounts receive the same digest and PDF with only the single preview recommendation and an upgrade note — paid plans get the full content.
</Callout>

## Weekly email digest

Every Monday at **8 AM local time**, restaurants receive a plain-HTML email digest with:

- The top recommendations and their projected impact (single preview on Free)
- Key metric changes from the previous week
- Quick-action links into the dashboard

## Monthly PDF deep dive

On the first of the month at **8 AM local time**, restaurants receive a full PDF report:

- The complete menu matrix visualization
- An item-by-item analysis table
- Month-over-month trend charts
- All AI recommendations with rationale (single preview on Free)
- An executive summary — the "you're leaving $X/mo on the table" number

## Timezone-aware scheduling

Each restaurant declares a timezone. The dispatch tasks check the **local hour** per restaurant — so a West Coast restaurant gets its digest at 8 AM PT, not 8 AM UTC. Dispatchers run hourly and deduplicate:

- Weekly digests are skipped if one was already sent in the last 5 days
- Monthly reports are skipped if one was already sent in the last 27 days

## Delivery

| Detail             | Value                                                          |
| ------------------ | -------------------------------------------------------------- |
| **Weekly digest**  | HTML email via Resend                                          |
| **Monthly report** | PDF generated with WeasyPrint, attached to an email via Resend |
| **Sender**         | `reports@menuwright.com`                                       |

If the PDF renderer is unavailable, generation **fails loudly** rather than sending an HTML file disguised as a PDF.

## Related

<Cards>
  <Card icon={<ChartLine />} title="Billing and usage" href="/menuwright/billing">
    Pro unlocks reports; see the plan limits.
  </Card>

  <Card icon={<Target />} title="Menu matrix" href="/menuwright/menu-matrix">
    What the reports analyze.
  </Card>

  <Card icon={<Rocket />} title="Getting started" href="/menuwright/getting-started">
    Set your restaurant timezone during onboarding.
  </Card>
</Cards>
